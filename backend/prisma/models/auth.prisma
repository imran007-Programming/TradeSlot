model User {
  id        String   @id @default(cuid())
  email     String?  @unique
  phone     String?  @unique
  password  String
  role      Role     @default(TRADER)
  createdAt DateTime @default(now())

  trader Trader?
}
