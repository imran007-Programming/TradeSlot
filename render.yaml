services:
  - type: web
    name: tradeslot-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install && npx prisma generate --schema=prisma/models && npm run build
    startCommand: node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
